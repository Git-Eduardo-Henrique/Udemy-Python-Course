cond = False
# None = variavel nula / sem tipo ou valor
passou = None

if cond:
    passou = True

# is None = verifica se é None
if passou is None:
    print("não passou do if")

# is not None = verifica se não é None
if passou is not None:
    print("passou do if")
